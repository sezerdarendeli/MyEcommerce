@model IEnumerable<Product>
@{
    ViewBag.Title = "Ürünler";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5></h5>
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>

                    </div>
                </div>
                <div class="ibox-content">
                    <div class="">
                        <a href="@Url.Action("Add", "Product")" class="btn btn-primary ">Yeni kayıt ekle</a>
                    </div>
                    <table class="table table-striped table-bordered table-hover dataTables-example" id="categoryList">
                        @Html.Partial("_TableList", Model)
                    </table>

                </div>
            </div>
        </div>
    </div>

</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}



    <script type="text/javascript">
      
        
        function deleteProduct(productId) {
         
            $.post('/Product/DeleteProduct', { productId: productId }, function (data) {
                $('#categoryList').html(data);
        alert('Ürün silinmiştir.');
            });
        }

    </script>
}






